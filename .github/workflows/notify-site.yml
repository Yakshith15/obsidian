  # name: Notify personal-site
  # on:
  #   push:
  #     branches: [main]
  # jobs:
  #   dispatch:
  #     runs-on: ubuntu-latest
  #     steps:
  #       - uses: peter-evans/repository-dispatch@v3
  #         with:
  #           token: ${{ secrets.PERSONAL_SITE_PAT }}
  #           repository: Yakshith15/personal-site
  #           event-type: vault-updated
